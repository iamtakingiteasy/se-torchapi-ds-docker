name: CI

on:
  deployment:
  schedule:
    - cron: "0 0 * * *"

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
          token: ${{ secrets.PUSH_TOKEN }}
      - name: Try sync
        run: ls; ./sync.sh
